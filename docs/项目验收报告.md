# SentinelKB 项目验收报告

验收日期：2026-08-15

运行方式：Windows 11 + Python 3.12 + Docker Desktop + Neo4j 5.26.28

运行模式：在线模式（OpenAI 兼容对话模型）+ 本地词法检索；同时保留不依赖 API Key 的离线模式

## 验收结论

SentinelKB 的核心业务链路已经贯通，可以作为一个完整的 AI 安全知识库原型进行本地演示：

`安全文档上传 → 文档解析与分块 → 检索索引 → IOC/ATT&CK 抽取 → Neo4j 图谱 → 可追溯问答 → 安全研判工作台`

## 自动化测试

- Pytest：27 项全部通过，覆盖配置、API、工作流、安全研判、幂等入库、检索隔离和评测指标。
- 在线 RAG 固定评测：10/10 通过，关键事实覆盖率、来源命中率和无答案拒答准确率均为 100%。
- 在线评测延迟：P50 约 2.95 秒，P95 约 20.46 秒；延迟会随模型服务和中转站波动。
- 评测边界：结果只说明当前固定演示数据与问题集通过，不能表述为生产环境准确率。

## 端到端验收结果

| 检查项 | 结果 |
|---|---|
| FastAPI 健康检查 | `ok` |
| 运行模式 | `online`；离线模式可独立启动 |
| Chroma + 词法索引 | `ready`，验收时 2 条索引 |
| Neo4j 图谱 | `ready`，验收时 18 个实体、23 条关系 |
| LangGraph 工作流 | `ready` |
| 在线知识问答 | 返回答案、中文问答类型、置信度和可追溯来源 |
| 安全事件研判 | 可提取 IOC、映射 ATT&CK、生成风险等级与处置建议 |
| 产品前端 | 智能问答、文档入库、安全研判、系统概览均可用 |
| API 文档 | Swagger UI 可用 |

## 交付入口

- `启动项目.cmd`：启动 Neo4j，等待其健康后启动 FastAPI。
- `关闭项目.cmd`：停止 Neo4j，但保留图谱数据卷。
- `验收项目.cmd`：自动验证健康、问答、安全研判、检索索引和知识图谱。
- 控制台：<http://localhost:8080>
- API 文档：<http://localhost:8080/docs>
- Neo4j Browser：<http://localhost:7474/browser/>

## 已知边界

- 离线问答返回检索到的可追溯原文，不等同于大模型生成回答。
- 当前 ATT&CK 映射和风险评分属于可解释规则，覆盖演示场景，不代表完整生产规则库。
- 当前是单机原型，正式生产部署仍需身份认证、租户隔离、密钥管理、审计、恶意文件检测和监控告警。
