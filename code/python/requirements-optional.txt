# Install only when the corresponding extension is being developed.

# PostgreSQL / PGVector backend
pgvector>=0.3,<1
psycopg2-binary>=2.9,<3
sqlalchemy>=2,<3
langchain-community>=0.3,<1

# OCR, spreadsheets and richer document parsing
unstructured>=0.16,<1
pillow>=11,<13
pytesseract>=0.3.13,<1
pdf2image>=1.17,<2
pandas>=2.2,<3
openpyxl>=3.1,<4

# Local embedding model (large download)
sentence-transformers>=3,<6

# CDC / Kafka
confluent-kafka>=2.6,<4
watchdog>=6,<7
